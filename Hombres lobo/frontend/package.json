{
  "name": "front-castronegro",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    "test": "echo \"Tests del frontend ejecutándose...\" && exit 0"
  },
  "devDependencies": {
    "laravel-echo": "^2.2.6",
    "pusher-js": "^8.4.0",
    "typescript": "~5.9.3",
    "vite": "^7.2.2"
  },
  "dependencies": {
    "axios": "^1.13.2"
  }
}
